# these are the working inputs and outputs of the tooling here, not app resources: keep them
# out of Git LFS so a clone without it can still read them
*.png      !filter !diff !merge -text
*.gif      !filter !diff !merge -text
